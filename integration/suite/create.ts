import { check } from "../../core/check";

check("Sample should be equal to sample", "sample".length === 6);


// Create allowed
// Check toket got added

// Create from non authority not allowed

// Create with same oracle not allowed

// Should not be possible if not signed
