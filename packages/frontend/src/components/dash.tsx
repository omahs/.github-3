import "../styles/dash.css";
import React, { Component } from "react";

export default class Dash extends Component {
    render() {
        return (
            <div className="dash">
                Dashboard
            </div>
        );
    }
}