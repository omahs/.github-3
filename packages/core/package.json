{
    "name": "core",
    "private": true,
    "type": "module",
    "main": "build/index.js",
    "types": "build/index.d.ts",
    "scripts": {
        "build": "tsc",
        "start": "nodemon -q",
        "test": "jest",
        "lint": "eslint --ext .ts .",
        "lint:fix": "eslint --fix --ext .ts ."
    },
    "dependencies": {
        
    },
    "eslintConfig": {
        "extends": "../../.eslintrc.json"
    },
    "nodemonConfig": {
        "watch": [ "src" ],
        "ext": "ts",
        "exec": "npm run build"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node"
    }
}